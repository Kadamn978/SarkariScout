# Role: QA / Testing

**Owns:** `docs/10-test-plan.md`, test infrastructure, quality gates

**Responsibilities:**
- Coordinate testing across all levels (unit, integration, E2E, security)
- Ensure 95%+ test coverage on core logic
- Run browser matrix (Chromium full, Firefox/WebKit smoke)
- Generate test reports (JUnit XML format)
- Verify security checklist at each phase gate
- Run pentest scenarios before launch

**Test levels:**
1. **Unit** — individual functions/methods (80%+ coverage)
2. **Integration** — API flows with real DB (all flows)
3. **E2E** — Playwright real browsers (every 🔴/🟠 feature)
4. **Security** — OWASP ZAP + abuse probes (no HIGH/CRITICAL)
5. **Manual** — design review, browser matrix (each release)

**When to escalate:**
- Test coverage can't reach threshold
- Flaky tests that can't be stabilized
- Security findings that need architectural change
- Browser-specific bugs that need investigation

---

## Quality Gates (per phase)

- [ ] All unit tests pass
- [ ] All integration tests pass
- [ ] E2E passes in Chromium
- [ ] E2E smoke passes in Firefox/WebKit
- [ ] Coverage ≥ 95% statements
- [ ] Coverage ≥ 90% branches
- [ ] `npm audit` clean
- [ ] `gitleaks` clean
- [ ] OWASP ZAP baseline: no HIGH/CRITICAL
- [ ] Security checklist 100% pass

---

## Test Data Standards

- **Seed data:** demo user with realistic profile
- **Fixture emails:** Mailtrap project inbox (dev)
- **Time control:** frozen "now" for deterministic deadline tests
- **Dummy data only:** never real user credentials
- **Obviously fake:** test data should be clearly synthetic

---

## Browser Matrix

| Browser | Runs | Scope |
|---|---|---|
| Chromium | every PR + nightly | full suite |
| Firefox | nightly + release | smoke set |
| WebKit | nightly + release | smoke set |
| Mobile emulation | nightly | responsive + layout |

---

## Test Report Format (JUnit XML)

```xml
<testsuites>
  <testsuite name="module-name" tests="N" failures="0">
    <testcase name="test description" time="0.12"/>
  </testsuite>
</testsuites>
```
